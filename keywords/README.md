# Keyword files

Each file holds search patterns for ShodanHound — **one query per line, no spaces**.

A filename containing `python` is automatically scoped with `language:python`
during the search. Add more `.txt` files here and register them in the
`KEYWORD_FILES` array at the top of `shodanhound.mjs`.
